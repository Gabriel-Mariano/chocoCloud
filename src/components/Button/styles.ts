import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container:{
        width:'100%',
        height:36,

        borderRadius:5,

        justifyContent:'center',
        alignItems:'center'
    },
    textButton: {
        fontWeight:'bold'
    }
})

export { styles };